# Reference memory data retention

Saved references have no automatic short retention in this feature. They remain until the owner deletes them or a future explicit retention policy is introduced.

The existing temporary input retention applies only to `inputs/`. Operators must treat `references/` as durable user content and include it in the same backup/privacy/deletion governance used for other persistent application media.
