# Reference memory non-goals

This implementation does not create a public gallery, cross-user sharing, automatic long-term retention of every upload, image editing, semantic tagging/search, or generated thumbnail derivatives. Those are separate product domains.

Reference memory is deliberately a private reusable source-image library for the authenticated owner and compatible generation models.
